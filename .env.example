# Google Cloud Project Configuration
PROJECT_ID=it-automation-training
REGION=us-central1

# JWT Secret Key for Token Authentication
# Generate a secure secret key: openssl rand -base64 32
# Or use: python3 generate_jwt.py --generate-secret
JWT_SECRET_KEY=your-secret-key-here

# Cloud Run API Configuration (for /api/services endpoint)
GCP_PROJECT_ID=it-automation-training
GCP_REGION=us-central1

# Optional: Service Configuration Overrides
# SERVICE_NAME=gcp-bearer-auth-service
# JWT_SECRET_NAME=jwt-secret-key
# PORT=8080
# MEMORY=512Mi
# CPU=1
# MAX_INSTANCES=10
# MIN_INSTANCES=0
# TIMEOUT=300
# ALLOW_UNAUTHENTICATED=true
